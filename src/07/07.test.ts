/*
test('test', () => {

    let props ={
        name: 'foo',
        age: 12,
        lessons:[{title:'hi'}, {title:'hi'}],
    }
})

const age = props.age
const lessons = props.lessons
const{age, lessons}= props

const{age: a, lessons: l}= props //псевдоним через двоеточие

expect(age).toBe(12)
expect(lessons.length).toBe(2)

*/
